# 上线前技术体检：空岗发布平台

Date: 2026-06-23
Scope: migrations, permissions, data consistency, error handling, frontend build, API contracts, logs, sensitive information
Result: PASS with P2/P3 follow-ups

## Executive Summary

本轮技术体检未发现已验证的 P0/P1 阻塞项。后端核心 API 测试拆分执行全部通过，前端生产构建通过，Alembic 当前版本已到 head。发现的风险主要是 P2/P3：前端大 chunk warning、历史手工日志残留旧数据库缺列错误、passlib/bcrypt 兼容 warning、开发/演示脚本中存在固定账号密码。

本报告不声明 live 12-step browser E2E 已通过；T006 已把该项记录为上线前必须补强的验收证据。

## Test And Build Evidence

| Check | Command | Result |
| --- | --- | --- |
| Match API tests | `.\.venv\Scripts\pytest.exe -q tests\test_api\test_matches.py` | PASS, 27 passed in 45.32s |
| Jobs API tests | `.\.venv\Scripts\pytest.exe -q tests\test_api\test_jobs.py` | PASS, 20 passed in 29.17s |
| Applications API tests | `.\.venv\Scripts\pytest.exe -q tests\test_api\test_applications.py` | PASS, 11 passed in 24.21s |
| Messages API tests | `.\.venv\Scripts\pytest.exe -q tests\test_api\test_messages.py` | PASS, 13 passed in 27.77s |
| Base data API tests | `.\.venv\Scripts\pytest.exe -q tests\test_api\test_base_data.py` | PASS, 12 passed in 13.94s |
| Combined backend attempt | Same five files in one command | TIMEOUT at 124s without final result; split runs passed |
| Frontend build | `npm.cmd run build` | PASS, Vite built in 9.93s with chunk-size warning |

Backend evidence total for this audit: 83 selected API tests passed after splitting by module.

Frontend build output:

```text
vite v5.4.21 building for production...
2615 modules transformed.
dist/index.html 0.41 kB
dist/assets/index-D7bn4Vb7.css 21.99 kB
dist/assets/index-D9yKt8I0.js 554.82 kB
dist/assets/AdminRaApp-CdVWxsNX.js 775.43 kB
✓ built in 9.93s
warning: Some chunks are larger than 500 kB after minification.
```

## Migrations

| Item | Evidence | Result |
| --- | --- | --- |
| Alembic current | `.venv\Scripts\python.exe -m alembic current` returned `c8d0e2f4g608 (head)` | PASS |
| Alembic heads | `.venv\Scripts\python.exe -m alembic heads` returned `c8d0e2f4g608 (head)` | PASS |
| Migration history | `alembic history --verbose` shows a single chain from `421bc078883c` to `c8d0e2f4g608` | PASS |
| Domain migrations | Files cover jobs, applications, messages/contact exchanges, match rules, experiments, audits, standard positions, tag refs | PASS |

Observation: old manual logs contain `UndefinedColumnError` for `jobs.standard_position_id` and `seeker_profiles.standard_position_id`, but current Alembic state is head and selected API tests pass. Treat old logs as stale residual evidence, not a current blocker.

## Permissions

| Area | Evidence | Result |
| --- | --- | --- |
| Jobs | Recruiter-only create/update/submit/visitors; seeker-only history/favorites/subscriptions; admin-only review/list | PASS |
| Applications | Seeker-only create/list/detail; recruiter-only received list/detail/status/resume file; admin stats/list | PASS |
| Messages | Seeker/recruiter conversation access; seeker-only start/open; recruiter/admin contact exchange stats; peer cannot approve own exchange | PASS |
| Match Quality and rules | Admin-only rule configs, experiments, operation audits, quality summary, match audit list/detail; seeker/recruiter only for own match views | PASS |
| Release governance | `release_check` blocks archived/active/invalid/no dimensions/running experiment; publish records operation audit | PASS |

## Data Consistency

| Area | Evidence | Result |
| --- | --- | --- |
| Job review state | Job create/update/submit/review tests passed; service resets reviewer fields on resubmission and stores reviewer on admin review | PASS |
| Application state | Application tests passed; duplicate application and invalid status paths are guarded | PASS |
| Contact exchange | Message tests passed; requester cannot review own contact exchange; accepted exchange returns structured contacts | PASS |
| Match quality | Match tests passed; quality summary, segments, anomalies, suggestions, experiments, audits, and release governance are covered by selected tests | PASS |
| Base data/tag refs | Base data tests passed; standard positions/tags and operation logs have migration and API evidence | PASS |

## Error Handling

| Area | Evidence | Result |
| --- | --- | --- |
| API role errors | `require_role(...)` and service-level 403 checks are present across target APIs | PASS |
| Domain errors | 404/400/409/422 paths exist for missing jobs/applications/conversations/rules/profiles and invalid transitions | PASS |
| Frontend API errors | `src/services/api.js` throws backend detail/message and clears invalid token on 401 | PASS |
| Match Quality invalid filters | backend has `invalid_segment_type` and `invalid_created_range` error paths | PASS |

## Sensitive Information

| Finding | Evidence | Severity | Decision |
| --- | --- | --- | --- |
| `.env` exists locally | `git check-ignore -v backend/job-platform/.env` confirms `.env` is ignored by `backend/job-platform/.gitignore:33` | P2 | No secret content read or reported; acceptable for local dev |
| `.env.example` contains placeholder secrets | placeholders like `your-...`, empty OCR/Wechat/OSS keys | P3 | Acceptable example values |
| `docker-compose.dev.yml` contains `POSTGRES_PASSWORD=dev123` | dev compose only | P3 | Acceptable for local dev; not production |
| Seed script fixed credentials | `scripts/seed_rp401_demo.py` has demo admin/recruiter/seeker passwords | P2 | Must remain demo-only and never reused in production |
| Token strings in code/docs | `access_token` usage in auth, tests, docs, and browser scripts | P3 | Implementation references, not leaked token values |

Production config guardrails exist in `app/core/config.py`: production validation rejects default `SECRET_KEY`, `JWT_SECRET_KEY`, `PHONE_HASH_SECRET`, and missing `ENCRYPTION_KEY`.

## Logs

| Evidence | Severity | Decision |
| --- | --- | --- |
| `backend_8003_cmd.log` and several manual `.err.log` files include passlib trapped warning: `module 'bcrypt' has no attribute '__about__'` | P3 | Tests pass; warning should be cleaned by dependency pin/update later |
| `manual-backend-8003.node.log` contains old `UndefinedColumnError` for missing standard-position columns | P2 | Current Alembic is head; selected tests pass. Clean stale logs or rotate before release evidence collection |
| Logs include local request paths/statuses and stack traces | P2 | Logs are gitignored; do not use historical logs as launch evidence |

## API Contract Coverage

| Contract | Evidence | Result |
| --- | --- | --- |
| Core business loop | jobs/applications/messages tests passed; T006 maps 12-step dry-run evidence | PASS |
| Match Quality | matches tests passed; `/api/v1/matches/quality/summary` and admin UI evidence reviewed | PASS |
| Rule governance | release-check, publish, experiments, operation audits in API and service evidence | PASS |
| Base data | base data tests passed; migrations and admin pages exist | PASS |

## Blockers

No P0/P1 blockers found in this audit.

## Follow-up Tasks

| ID | Severity | Item | Owner Task |
| --- | --- | --- | --- |
| T008-FU-01 | P2 | Execute live browser 12-step E2E after demo seed plan is finalized | T010 |
| T008-FU-02 | P2 | Rotate or delete stale local manual logs before collecting release evidence | T012 or release prep |
| T008-FU-03 | P2 | Keep demo seed credentials scoped to local/demo only and document cleanup | T010 |
| T008-FU-04 | P3 | Consider dependency pin/update to remove passlib bcrypt version warning | Future maintenance |
| T008-FU-05 | P3 | Consider manual chunks/code-splitting for `AdminRaApp` and main bundle | Future frontend optimization |

## Final Decision

PASS for T008 documentation and pre-release technical audit scope. Continue to T009 because no current P0/P1 blocker was proven. The live E2E gap remains P2 and is already routed to T010.
