# 前后端联调实施方案

## Context（背景）

**当前状态**：
- ✅ **后端**：认证和用户模块已完成，pytest全绿（28/28）
- ✅ **前端**：原型UI完整，使用mock数据
- ❌ **未联调**：前端尚未连接真实后端API

**问题**：
- 前端原型所有数据都是mock，无法持久化
- 后端API已就绪但无前端调用
- 需要验证前后端接口契约

**目标**：
- 让前端原型连接真实后端API
- 优先打通认证闭环（注册、登录、用户信息）
- 保留原型演示能力（mock/real双模式）
- 不大改UI，集中改数据层

---

## 联调范围

### ✅ 第一阶段：认证闭环（本次实施）

**后端API**（已完成）：
1. `POST /api/v1/auth/send-verification-code` - 发送验证码
2. `POST /api/v1/auth/register` - 用户注册
3. `POST /api/v1/auth/login` - 用户登录
4. `GET /api/v1/users/me` - 获取当前用户
5. `PUT /api/v1/users/me` - 更新用户信息

**前端页面**（需改造）：
1. `src/seeker/SeekerPages.jsx` - 应聘者注册
2. `src/recruiter/RecruiterPages.jsx` - 招聘者注册
3. 登录功能（需新增）
4. 用户信息展示和编辑

### ⚠️ 第二阶段：其他模块（后续实施）

**暂不联调**（后端API未完成）：
- 岗位管理（CRUD、搜索）
- 投递管理
- 聊天消息
- 通知中心
- 画像生成
- 数据统计

---

## 技术方案

### 1. 统一API Client

**创建**：`src/services/api.js`

**功能**：
- 统一请求封装
- Token管理（自动携带）
- 错误处理（401/403跳转）
- 环境变量配置

**架构**：
```
src/services/
├── api.js          # 真实API封装
├── mock.js         # Mock数据（保留）
├── auth.js         # 认证服务
├── user.js         # 用户服务
└── index.js        # 统一导出（根据环境变量切换）
```

### 2. Mock/Real双模式

**环境变量控制**：
```bash
# .env.development - 联调模式
VITE_USE_MOCK=false
VITE_API_BASE_URL=http://localhost:8001

# .env.demo - 演示模式
VITE_USE_MOCK=true
```

**实现方式**：
```javascript
// src/services/index.js
const USE_MOCK = import.meta.env.VITE_USE_MOCK === 'true'

export const authService = USE_MOCK 
  ? mockAuthService 
  : realAuthService
```

### 3. Token管理

**存储**：localStorage（一期方案）
```javascript
// 登录成功
localStorage.setItem('access_token', token)
localStorage.setItem('user_info', JSON.stringify(user))

// 获取token
const token = localStorage.getItem('access_token')

// 登出
localStorage.removeItem('access_token')
localStorage.removeItem('user_info')
```

**自动携带**：
```javascript
// api.js中统一处理
headers: {
  'Authorization': `Bearer ${localStorage.getItem('access_token')}`
}
```

---

## 实施步骤

### Phase 1：基础设施（30分钟）

#### 1.1 安装依赖
```bash
cd D:\AIposition\frontend\wechat-prototype
# 无需安装新依赖，使用原生fetch
```

#### 1.2 创建API Client
**文件**：`src/services/api.js`

**功能**：
- `request()` - 统一请求封装
- 自动携带token
- 401/403处理
- 错误提示

#### 1.3 创建服务层
**文件**：
- `src/services/auth.js` - 认证服务
- `src/services/user.js` - 用户服务
- `src/services/mock.js` - Mock服务（保留原有mock数据）
- `src/services/index.js` - 统一导出

#### 1.4 环境变量配置
**文件**：
- `.env.development` - 联调配置
- `.env.demo` - 演示配置

### Phase 2：后端准备（15分钟）

#### 2.1 验证数据库迁移
```bash
cd D:\AIposition\backend\job-platform

# 在真实PostgreSQL测试库执行
poetry run alembic upgrade head

# 验证表创建成功
psql -U user -d jobplatform_test -c "\dt"
```

#### 2.2 验证Redis
```bash
# 确认Redis运行
redis-cli ping
# 应返回：PONG
```

#### 2.3 配置CORS
**文件**：`backend/job-platform/.env`
```bash
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175
```

#### 2.4 启动后端
```bash
cd D:\AIposition\backend\job-platform
poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
```

**验证**：访问 http://localhost:8001/docs

### Phase 3：前端改造 - 注册流程（45分钟）

#### 3.1 应聘者注册
**文件**：`src/seeker/SeekerPages.jsx`

**改造点**：
- `SeekerRegister` 组件
- 替换mock验证码为真实API
- 调用真实注册API
- 存储token和用户信息
- 注册成功跳转

**关键API**：
1. 发送验证码：`POST /api/v1/auth/send-verification-code?phone=xxx`
2. 注册：`POST /api/v1/auth/register`

#### 3.2 招聘者注册
**文件**：`src/recruiter/RecruiterPages.jsx`

**改造点**：
- `RecruiterRegister` 组件
- 上传营业执照（一期mock OCR）
- 调用真实注册API
- 存储token

### Phase 4：前端改造 - 登录流程（30分钟）

#### 4.1 创建登录页面
**文件**：`src/common/Login.jsx`（新建）

**功能**：
- 手机号+密码登录
- 调用登录API
- 存储token
- 跳转到对应角色首页

#### 4.2 添加登录路由
**文件**：`src/App.jsx`

**添加路由**：
- `/login` - 登录页面
- 未登录时跳转登录

### Phase 5：前端改造 - 用户信息（30分钟）

#### 5.1 应聘者个人中心
**文件**：`src/seeker/SeekerPages.jsx`

**改造点**：
- 从API获取用户信息（`GET /users/me`）
- 更新用户信息（`PUT /users/me`）
- 显示真实手机号、姓名

#### 5.2 招聘者个人中心
**文件**：`src/recruiter/RecruiterPages.jsx`

**改造点**：
- 获取用户信息
- 更新企业信息

### Phase 6：测试验证（30分钟）

#### 6.1 端到端测试

**测试用例**：
1. ✅ 应聘者注册
   - 发送验证码
   - 填写信息
   - 注册成功
   - 跳转到首页

2. ✅ 应聘者登录
   - 输入手机号密码
   - 登录成功
   - 显示用户信息

3. ✅ 更新用户信息
   - 修改显示名称
   - 上传头像
   - 保存成功

4. ✅ 招聘者注册
   - 上传营业执照
   - 填写信息
   - 注册成功

5. ✅ Token失效处理
   - 删除token
   - 访问需认证页面
   - 自动跳转登录

#### 6.2 Mock模式测试

**切换到Mock模式**：
```bash
# .env
VITE_USE_MOCK=true
```

**验证**：
- 原型演示功能正常
- 所有页面可访问
- 不依赖后端

---

## 文件清单

### 新增文件（6个）

**服务层**：
1. `src/services/api.js` - API Client
2. `src/services/auth.js` - 认证服务
3. `src/services/user.js` - 用户服务
4. `src/services/mock.js` - Mock服务
5. `src/services/index.js` - 统一导出

**组件**：
6. `src/common/Login.jsx` - 登录页面

**配置**：
7. `.env.development` - 开发配置
8. `.env.demo` - 演示配置

### 修改文件（4个）

1. `src/seeker/SeekerPages.jsx` - 应聘者注册/个人中心
2. `src/recruiter/RecruiterPages.jsx` - 招聘者注册/个人中心
3. `src/App.jsx` - 添加登录路由
4. `backend/job-platform/.env` - 配置CORS

---

## 验证步骤

### 1. 后端准备
```bash
# 1. 数据库迁移
cd D:\AIposition\backend\job-platform
poetry run alembic upgrade head

# 2. 启动后端
poetry run uvicorn app.main:app --reload --port 8001

# 3. 验证API文档
# 访问 http://localhost:8001/docs
```

### 2. 前端启动
```bash
# 1. 配置环境
cd D:\AIposition\frontend\wechat-prototype
cp .env.development .env

# 2. 启动前端
npm run dev

# 3. 访问
# http://localhost:5174
```

### 3. 联调测试
1. 打开浏览器 http://localhost:5174
2. 选择"应聘者"
3. 点击注册
4. 发送验证码（真实API）
5. 填写信息注册
6. 验证token存储
7. 查看个人信息

### 4. Mock模式测试
```bash
# 修改.env
VITE_USE_MOCK=true

# 重启前端
npm run dev

# 验证原型演示功能正常
```

---

## 注意事项

### ⚠️ 保护原型
- ✅ 创建Git分支或复制baseline
- ✅ 只修改服务层和数据调用
- ❌ 不要重构UI组件
- ❌ 不要改动未联调模块

### ⚠️ 渐进式接入
- ✅ 优先认证闭环
- ✅ 保留mock/real双模式
- ✅ 未完成模块继续mock
- ❌ 不要硬接假接口

### ⚠️ 错误处理
- ✅ 401自动跳转登录
- ✅ 网络错误友好提示
- ✅ 后端错误信息展示
- ✅ Loading状态

### ⚠️ 安全考虑
- ⚠️ Token存localStorage（一期方案）
- ⚠️ 生产环境需考虑XSS防护
- ⚠️ HTTPS传输
- ⚠️ Token刷新机制（二期）

---

## 时间估算

| 阶段 | 任务 | 预计时间 |
|------|------|----------|
| Phase 1 | 基础设施（API Client + 服务层） | 30分钟 |
| Phase 2 | 后端准备（验证+启动） | 15分钟 |
| Phase 3 | 注册流程改造 | 45分钟 |
| Phase 4 | 登录流程实现 | 30分钟 |
| Phase 5 | 用户信息改造 | 30分钟 |
| Phase 6 | 测试验证 | 30分钟 |
| **总计** | | **~3小时** |

---

## 后续规划

### 第二阶段：岗位模块联调
**前提**：后端岗位API完成

**联调内容**：
- 岗位发布
- 岗位列表
- 岗位详情
- 岗位搜索

### 第三阶段：投递模块联调
**前提**：后端投递API完成

**联调内容**：
- 投递简历
- 投递记录
- 投递状态更新

### 第四阶段：其他模块
- 聊天消息
- 通知中心
- 画像生成
- 数据统计

---

## 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| CORS配置错误 | 前端无法请求后端 | 仔细配置ALLOWED_ORIGINS |
| Token格式不匹配 | 认证失败 | 对照API文档验证 |
| 手机号格式差异 | 注册失败 | 前后端统一验证规则 |
| 原型功能回退 | 演示受影响 | 保留mock模式 |
| 后端API未完整 | 联调受阻 | 优先完成认证API |

---

## 成功标准

### ✅ 功能完成
- [ ] 应聘者可以注册、登录
- [ ] 招聘者可以注册、登录
- [ ] 用户信息正确展示
- [ ] 用户资料可以更新
- [ ] Token失效自动跳转登录

### ✅ 质量保证
- [ ] Mock模式正常运行
- [ ] Real模式API调用成功
- [ ] 错误处理友好
- [ ] Loading状态正确

### ✅ 代码质量
- [ ] API调用集中在服务层
- [ ] 不在组件中散写fetch
- [ ] Token管理统一
- [ ] 环境变量配置清晰

---

**预计完成时间**：3小时  
**优先级**：高  
**建议开始时间**：后端验证通过后立即开始
