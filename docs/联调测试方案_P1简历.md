# 简历解析底座 - 联调测试方案

> 测试日期：2026-06-15
> 测试目标：验证 P1 简历上传 + 解析底座的前后端联调

---

## 一、测试前准备

### 1.1 确认服务运行

| 服务 | 地址 | 检查方式 |
|------|------|---------|
| 前端 | http://localhost:5174 | 浏览器打开能看到落地页 |
| 后端 | http://localhost:8003/docs | 能看到 API 文档 |
| 数据库 | - | 已确认 healthy |

### 1.2 测试账号

| 角色 | 手机号 | 密码 |
|------|--------|------|
| 求职者 | `13834295528` | `Test1234` |

### 1.3 准备测试文件

需要一份 `.docx` 格式的简历文件用于上传测试。
- ✅ 支持：`.docx`
- ⚠️ 部分支持：`.pdf`（可能进 OCR 队列）
- ❌ 不支持：`.doc`（旧格式）

如果没有现成简历，可以用 Word 随便写一份，包含：姓名、电话、邮箱、工作经历、教育经历、技能。

---

## 二、测试范围说明（重要）

当前联调进度，分两部分：

### ✅ 已接真实后端（可真实测试）
- 简历上传（POST /api/v1/resumes/me/upload）
- 简历状态查询（GET /api/v1/resumes/me）

### ⚠️ 仍是 Mock 数据（界面能看，数据是假的）
- 解析历史列表页
- 解析详情页（原文预览）

> 说明：R-P1-06 联调尚未完成，解析历史/详情页接真实接口是下一步工作。本次主要验证**上传流程**是否打通。

---

## 三、测试步骤（照着点）

### 测试 1：登录求职者账号

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1.1 | 浏览器打开 http://localhost:5174 | 看到落地页（角色选择） |
| 1.2 | 强制刷新（Ctrl+Shift+R） | 确保加载最新代码 |
| 1.3 | 点"我是求职者" → 找到登录入口 | 进入登录页 |
| 1.4 | 输入手机号 `13834295528`，密码 `Test1234` | 登录成功，进入应聘者首页 |

**❓ 如果登录失败**：
- 检查是否提示 "Failed to fetch" → 说明后端没连上
- 检查账号是否存在 → 可能需要重新注册

---

### 测试 2：查看简历状态（GET /me）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.1 | 底部点"我的"（个人中心） | 看到个人中心页 |
| 2.2 | 找到"上传简历"那一行 | 右侧显示状态文案 |

**预期状态文案**：
- 没传过简历 → "未上传"
- 传过简历 → "已解析" 或 "待解析"

**✅ 这步验证**：`getMyResume()` 接口能正常返回状态

---

### 测试 3：上传简历（POST /upload）⭐ 核心

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.1 | 点"上传简历"进入上传页 | 看到上传页面 |
| 3.2 | 点"选择并上传简历" | 弹出文件选择框 |
| 3.3 | 选一份 .docx 简历 | 显示"上传中..." |
| 3.4 | 等待上传完成 | 提示"简历已上传 ✓" |
| 3.5 | 上传后页面变化 | 显示"重新上传"和"查看简历画像"按钮 |

**✅ 这步验证**：
- `uploadResume()` 接口能上传文件
- 后端创建了 upload 记录和 parse_run 任务
- 文件保存到 `backend/job-platform/uploads/`

**❓ 如果上传失败**：
- 记下错误提示文案
- 可能原因：文件格式不支持、后端解析报错

---

### 测试 4：验证后端数据（我帮你查）

上传后，告诉我"上传成功了"，我会帮你查数据库确认：
- resume_uploads 表有没有新记录
- resume_parse_runs 表有没有解析任务
- resume_extracted_texts 有没有原文
- resume_chunks 有没有切片

---

### 测试 5：解析历史页（⚠️ 当前是 Mock）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 5.1 | 简历画像页点"查看解析历史" | 看到历史列表 |
| 5.2 | 查看列表内容 | ⚠️ 显示的是 3 条 mock 数据 |
| 5.3 | 点任意一条 | 进入详情页，看到 mock 原文 |

**说明**：这页现在是假数据，只验证 **UI 布局**是否正常。真实数据等 R-P1-06 联调完成。

---

## 四、测试结果记录表

测试时把结果填这里，发给我：

| 测试项 | 结果 | 备注 |
|--------|------|------|
| 1. 登录 | ⬜ 通过 / ⬜ 失败 | |
| 2. 查看状态 | ⬜ 通过 / ⬜ 失败 | 状态文案：____ |
| 3. 上传简历 | ⬜ 通过 / ⬜ 失败 | 提示文案：____ |
| 5. 历史页UI | ⬜ 正常 / ⬜ 异常 | |

---

## 五、常见问题排查

| 现象 | 可能原因 | 解决 |
|------|---------|------|
| Failed to fetch | 后端没连上 | 确认 8003 在跑 |
| 登录失败 | 账号不存在 | 换账号或重新注册 |
| 上传无反应 | 前端缓存旧代码 | Ctrl+Shift+R 强刷 |
| 上传报 500 | 后端解析报错 | 截图错误，发给我 |
| 页面空白 | JS 报错 | F12 看控制台，截图发我 |

---

## 六、你只需要做的事（简化版）

如果不想看上面那么细，就按这 4 步：

1. **打开** http://localhost:5174，**强制刷新**（Ctrl+Shift+R）
2. **登录**：求职者 `13834295528` / `Test1234`
3. **上传简历**：我的 → 上传简历 → 选一份 .docx
4. **告诉我结果**：成功了？还是哪一步报错？（报错就截图）

我会根据你的反馈，帮你查后端数据 + 修复问题。
