# 实施方案 —— 29 条需求落地 & PRD 回写计划

| 项 | 内容 |
|----|------|
| 来源 | `需求清单_语音记录20260603.md`（语音转写提取，29 条） |
| 现状基准 | `wechat-prototype/`（React + Vite 纯前端 mock 原型，双端共 21 页） |
| 对标 PRD | `PRD_空岗信息发布对接平台v2.md`（V2.1，1700 行，BR-/US-/AC-/AI- 编号体系） |
| 产出日期 | 2026-06-03 |
| 原则 | 码上与 PRD 同步——每条代码改动同时标注 PRD 回写位置与编号，不做完代码再补文档 |

---

## 一、总体方案概览

这 29 条在原型里的落地可分三种操作类型：

| 类型 | 说明 | 数量 |
|------|------|:---:|
| **新建页面/组件** | 需要新文件或扩展现有文件成独立模块 | 6 |
| **在现有页面里改造** | 改状态/加入口/调交互，不新建文件 | 18 |
| **不写代码** | 已做 / 暂不做 / 非产品 | 5 |

### 分阶段实施

| 阶段 | 内容 | 预估改动量 |
|:---:|------|:---:|
| **P0：修 Bug + 核心链路** | A2+A3(投递门槛)、B8(候选人列表)、B9(浏览量穿透)、B12(人才池) | 大（~3 新页 + 多处改造） |
| **P1：画像数据流串联** | A1(企业画像入口)、B1(注册生成企业画像)、B2(上传JD)、B3(代写输入企业画像)、B4(发布时生成岗位画像)、B7(画像可编辑) | 中（~1 新页 + 多处串联） |
| **P2：统计/排序/筛选增强** | A4(推送统计)、A5(状态标识)、A6(排序筛选)、B5(薪资建议增强)、B10(岗位排序状态)、B11(总体统计) | 中（改现有页） |
| **P3：AI 增值 + 交互增强** | A7(订阅画像)、A9(AI 简历优化)、A10(薪酬测算)、A11(测评学习入口)、B13(推送测评)、B14(双向标签) | 中（改现有页） |
| **不写码** | A8(中转通讯-未来)、A12(✅)、B6(✅)、B15(✅)、B16/B17(⏸)、C1/C2(非产品) | - |

---

## 二、逐条实施方案（附 PRD 回写标注）

### A1. 岗位详情里企业画像可点击查看

- **类型**: ➕新增入口
- **优先级**: 高
- **代码改动**:

  | 文件 | 改动 |
  |------|------|
  | `src/seeker/SeekerFlow.jsx` #44-49 | 在 company cell 外包 `onClick`，跳转企业画像页 |
  | `src/App.jsx` #111-123 | 新增路由 `/seeker/company-portrait/:id` |
  | `src/seeker/SeekerExtra.jsx`（或新建 SeekerPortrait 复用组件） | 新增 `SeekerCompanyView`——复用 `RecruiterCompanyPortrait` 的数据，但去除招聘者专属操作按钮，只读展示 |

- **Mock 数据**: `companyProfile`（`mock/data.js` #291-307）已存在，足够使用。需为不同企业准备多份画像——当前只有一份"星辰互联"，需在 mock 中按 `job.companyShow` 映射画像数据。
- **PRD 回写**:

  | 位置 | 编号建议 | 回写内容 |
  |------|---------|---------|
  | 3.2.4 查看职位完整信息 | BR-A-036 后新增 BR-A-036a | 【新增】应聘者可在岗位详情页点击企业名称/Logo 查看该企业的 AI 生成画像（仅包含公开维度：规模实力/福利待遇/成长空间/技术氛围/稳定性/雇主口碑），帮助应聘者评估企业匹配度 |
  | 3.2.4 交互流程 Step 7 后 | - | 新增 Step 7a：应聘者可在完整信息页点击企业名称区域查看企业画像 |
  | 6.9 验收 | AC-A-032 后新增 AC-A-033 | AC-A-033：应聘者在岗位详情页点击企业名称→跳转该企业的公开画像页→展示企业标签+维度评分+AI 企业总结 |

---

### A2. 补充信息页内置简历上传

- **类型**: 🔧整合到补信息流
- **优先级**: 高
- **代码改动**:

  | 文件 | 改动 |
  |------|------|
  | `src/seeker/SeekerFlow.jsx` #94-143 `SeekerProfileEdit` | 在期望信息模块下方新增"上传简历"区域（复用 `SeekerResumeUpload` 的上传→解析→预填逻辑）。增加 `stage` 状态管理（idle/parsing/done），解析完成后自动将技能/经历填入能力信息字段 |
  | `src/seeker/SeekerPortrait.jsx` #8-73 `SeekerResumeUpload` | 保留独立页面的上传入口（「我的」下仍可独立上传），但增加逻辑判断：如果来自补信息流（URL 参数 `?from=edit`），完成后导航回 `/seeker/profile/edit` |

- **关键交互**: 在补信息页上传简历后，AI 解析出的技能标签自动填充到能力信息模块，减少重复填写。
- **Mock 数据**: `resumeParsed`（`mock/data.js` #259-268）已存在。
- **PRD 回写**:

  | 位置 | 编号 | 回写内容 |
  |------|------|---------|
  | 3.2.4 能力信息 BR-A-033 | BR-A-033a | 【新增】应聘者可在「补充信息」页面上传简历（PDF/图片），AI OCR 解析后自动填充学历、工作经历、技能标签等字段。此上传与「我的→上传简历」为同一入口，解析结果双向同步 |
  | 3.2.4 交互流程 Step 3 Tab3 | - | 在能力信息 Tab 中增加「上传简历，AI 自动填充」操作区 |
  | 附录 C 应聘者信息字段 | - | 新增字段「简历文件」类型：文件，必填：否 |
  | 附录 D AI 功能清单 | #34（简历解析与结构化） | 优先级从 P0 二期调为 P0 一期（一期就做上传+解析，不做深度结构化） |

> **注意**: PRD 目前将"AI简历解析"列在 7.2 二期（#34），但一期原型已有 mock 上传解析流程。建议 PRD 中把"上传+解析预填"升为一期 P0，保留"深度结构化存储"为二期。

---

### A3. 信息不全不让投递 + unlocked bug 修复

- **类型**: 🔧改逻辑
- **优先级**: 高
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/seeker/SeekerFlow.jsx` | #13 `useState(false)` | **fix**: 改为从全局状态读取——新增 `profileCompleted` 标志。用一个简单的 context 或 mock 全局变量（如 `window.__profileCompleted`）来模拟"是否已完成补信息" |
  | `src/seeker/SeekerFlow.jsx` | #17-19 `tryViewFull` | 跳转补信息页后，补信息页提交时回写 `unlocked`。增加 URL 回调逻辑：`navigate('/seeker/profile/edit?from=job&id='+job.id)` → 补信息页提交后 `navigate('/seeker/job/'+job.id, { state: { unlocked: true } })` |
  | `src/seeker/SeekerFlow.jsx` | #67-72 解锁提示 | 当 `unlocked=true` 时不展示 |
  | `src/seeker/SeekerFlow.jsx` | #84-88 底部按钮 | 投递按钮改为：先检查 unlock + 简历是否上传，任一条件不满足则引导补信息；都满足才允许跳转投递页 |
  | `src/seeker/SeekerFlow.jsx` | #94-143 `SeekerProfileEdit` | 提交时写 `unlocked=true`。提交条件加：必须上传简历（A2 整合后的新约束） |

- **核心逻辑**: 投递前置门槛 = 必填字段已填 + 简历已上传 → 两个条件都满足才 `unlocked=true`，才能解锁完整详情和投递按钮。
- **Mock 数据**: 新增 `window.__mockProfileState = { completed: false, hasResume: false }` 或直接用一个简单的 React context。
- **PRD 回写**:

  | 位置 | 编号 | 回写内容 |
  |------|------|---------|
  | 3.2.4 BR-A-036 | 修改 BR-A-036 | 原：「基本信息和联系方式中至少各一项设为"对招聘者可见"，否则无法解锁完整岗位信息」→ 改为：「基本信息和联系方式中至少各一项设为"对招聘者可见"**且已上传简历**，否则无法解锁完整岗位信息与投递功能」 |
  | 3.2.4 BR-A-037 | 修改 BR-A-037 | 「信息补充完成后，应聘者可查看岗位完整信息」→ 增加「并可在线投递简历」 |
  | 3.2.6 BR-A-050 | 修改 BR-A-050 | 「留言需补充个人信息后方可发送」→ 增加「投递需额外完成简历上传后方可操作（一期：完成投递需补充信息+上传简历）」 |
  | 6.8 AC-A-032 | 修改 AC-A-032 | 增加验收条件：简历未上传时投递按钮不可点击，提示"请先上传简历" |
  | 3.2.4 交互流程 Step 2 | - | 系统检查项增加「是否已上传简历」 |

---

### A4. 首页推送岗位统计

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/seeker/SeekerPages.jsx` | #30-41 `Feed` | 在搜索栏和 AI 排序提示之间新增一行：「⚡ AI 已为你累计推送 **{count}** 个岗位」，count 从 `feedJobs.length` 或新增 mock 字段读取 |
  | `src/mock/data.js` | - | 新增 `seekerPushStats = { totalPushed: 47, todayPushed: 3 }` |

- **PRD 回写**: 位置 3.2.3，在 BR-A-020 后新增 BR-A-020a：【新增】应聘者首页展示"AI 累计推送岗位数"统计。轻量，不涉及新增 BR 编号也可以直接附在交互流程描述中。

---

### A5. 岗位/消息状态标识

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/mock/data.js` | feedJobs 各条目 | 为每条 `feedJob` 新增字段：`status: 'new' \| 'viewed' \| 'chatted' \| 'archived' \| 'starred'` |
  | `src/seeker/SeekerPages.jsx` | Feed | 岗位卡片增加状态标识图标（👁 已读 / 💬 已聊过 / 🗑 已放弃 / ⭐ 重点收藏），在卡片右上角展示 |
  | `src/seeker/SeekerFlow.jsx` | SeekerJobDetail | 查看岗位时自动将状态由 `new` → `viewed` |
  | `src/seeker/SeekerExtra.jsx` | SeekerFavorites | 收藏夹支持「重点收藏」标记（区别于普通收藏，普通收藏在 `favoriteJobs` 中，重点收藏在岗位流中有视觉强化） |

- **PRD 回写**:

  | 位置 | 编号 | 回写内容 |
  |------|------|---------|
  | 3.2.3 接收信息 | BR-A-020 后新增 BR-A-020b | 【新增】岗位/消息支持状态标识：新推送(未读)、已查看、已留言沟通、已放弃(不再推荐)、重点收藏。状态变更由用户操作触发或 AI 根据行为自动标记 |
  | 5.1.1 埋点 | 新增 E022 | job_status_change：用户变更岗位状态 |

---

### A6. 岗位多维排序/筛选

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/seeker/SeekerPages.jsx` | Feed #31-68 | 在 AI 排序提示下方增加一排筛选条件（横滑 chips）：按匹配度降序 / 按时间新→旧 / 按薪资高→低 / 仅看收藏。选中后对 `feedJobs` 做 `sort/filter` |
  | `src/mock/data.js` | - | 无需新增（排序在前端做） |

- **PRD 回写**: 位置 3.2.3，在 3.2.3 末尾新增筛选/排序业务规则 BR-A-020c：【新增】应聘者可在首页按以下维度排序岗位列表：匹配度降序（默认，基于 AI 匹配度评分）、发布时间（新→旧）、薪资范围（高→低）。筛选条件：仅看收藏/仅看未读/仅看高匹配（>=80%）。

---

### A7. 订阅画像概念调整

- **类型**: 🔧调整
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/seeker/SeekerPages.jsx` | Subscriptions #71-135 | (1) 标题改为「订阅画像」；(2) 每段自然语言解析后生成的订阅展示为"画像卡片"而非简单列表——卡片含关键词+城市+薪资+**匹配岗位数预览**；(3) 增加"AI 基于你已补充的信息，推荐你能胜任的岗位"区域（区别于"你想要的"），来源是 `seekerProfile` 的 `dims` 数据 |
  | `src/mock/data.js` | initialSubs | 为每条订阅新增字段：`matchCount`（基于该画像已匹配到的岗位数）、`source: 'manual' \| 'ai_recommend'` |

- **PRD 回写**: 位置 3.2.2，修改 BR-A-010：将"关键词订阅"概念调整为"订阅画像"——每段自然语言解析后形成一个订阅画像（含关键词+城市+薪资+匹配岗位数），上限 10 个。AI 推荐基于用户已补充信息推"你能胜任的岗位"（区别于"你想要的"），用户可采纳为订阅画像。

---

### A8. 中转通讯（未来）

- **类型**: ⏸未来
- **不写代码**。
- **PRD 回写**: 在 BR-X-001 末尾加备注「未来规划」：三期考虑平台中转通讯（虚拟电话/短信转发/视频面试），不在一二期范围。

---

### A9. 简历 AI 优化建议

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/seeker/SeekerPortrait.jsx` | SeekerResumeUpload #8-73 | 解析完成（`stage === 'done'`）后，在技能标签下方新增「⚡ AI 简历优化建议」卡片，内容 mock 为几条建议如："建议补充项目量化成果""教育经历可突出 GPA / 排名""技能描述可与岗位 JD 对齐" |
  | `src/mock/data.js` | - | 新增 `resumeOptimizationTips = ['建议补充项目量化成果（如"日活提升 30%"）', '教育经历可增加 GPA 或排名信息', '技能标签建议补充框架版本号（如 React 18）']` |

- **PRD 回写**: 位置 3.2.4，BR-A-038b 后新增 BR-A-038c：【新增】AI 简历优化：简历上传并解析后，AI 对简历内容进行分析并给出 3-5 条优化建议（覆盖：量化成果缺失、技能描述精度、结构完整性等维度）。建议以"AI 优化建议"卡片展示在解析结果页。

---

### A10. 薪酬测算（个人画像内）

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/seeker/SeekerPortrait.jsx` | SeekerPortrait #84-120 | 在 AI 提升建议卡片上方新增「💰 AI 薪酬测算」卡片：基于画像的 `dims` 数据，展示估算月薪范围（如"AI 测算您的市场价值约为 **18K-28K**/月（深圳·前端开发·5年经验）"），附说明"基于画像维度+市场数据综合测算，仅供参考" |
  | `src/mock/data.js` | seekerProfile | 新增字段 `salaryEstimate: { low: 18, high: 28, basis: '深圳·前端开发·5年经验·本科' }` |

- **PRD 回写**: 位置 3.2.4，BR-A-038c 后新增 BR-A-038d：【新增】AI 薪酬测算：应聘者在个人画像页可查看 AI 基于其技能/经验/城市/学历综合测算的市场薪酬水平。测算结果以区间展示，标注"AI 测算，仅供参考"。

---

### A11. 「我的」加测评 + 学习入口

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/seeker/SeekerPages.jsx` | Profile #162-201 | 在「我的」CellGroup 中新增两个菜单项：「📝 申请测评」（标注"即将上线"）和「📚 参加学习」（标注"即将上线"），点击弹出 toast "功能即将上线" |

- **PRD 回写**: 位置 3.2.8，BR-A-071 快捷入口列表增加两项："申请测评入口""学习课程入口"（一期为占位入口，内容二期上线）。

---

### A12. 岗位转发裂变 ✅

- **类型**: ✅已有
- **不写代码**。`SeekerFlow.jsx` 详情页分享按钮（#24-25 `showShare` + `ShareSheet`）已做，`SeekerExtra.jsx` `ShareSheet`（#141-156）可复用。
- **PRD 回写**: 3.2.5 已有 BR-A-040~043 覆盖，无需补充。

---

### B1. 注册后自动生成企业画像

- **类型**: 🔗串联
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterPages.jsx` | RecruiterRegister #156-225 | 注册成功后（#219 `onClick`），在 toast 后增加跳转逻辑：先跳转到一个简短的"AI 正在生成企业画像…"过渡态，然后跳转企业画像页 `/recruiter/company-portrait?fresh=true` |
  | `src/recruiter/RecruiterPortrait.jsx` | RecruiterCompanyPortrait #10-43 | 支持 `?fresh=true` 参数——展示"AI 已为您生成初始企业画像"提示，引导用户查看和确认画像 |

- **PRD 回写**: 位置 3.1.1，BR-R-009 后新增 BR-R-009f：【新增】注册认证完成后，系统自动基于营业执照信息 + 行业 + 规模等字段生成初始企业画像（企业标签+维度评分），招聘者可在「我的→企业画像」查看和确认。画像生成在注册提交后异步完成，不影响发布权限。

---

### B2. 工作职责三种来源

- **类型**: ➕新增
- **优先级**: 高
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterFlow.jsx` | RecruiterJobCreate #97-118 | 在"工作职责"区域的 `AIButton` 旁新增一个「📄 上传 JD 文件」按钮（mock 行为：点击后模拟上传→AI 解析→自动填入 `duty` 和 `require` 字段）。增加 `uploading` 状态，上传后 1.5s 模拟解析完成 |
  | `src/recruiter/RecruiterFlow.jsx` | #6-7 | state 新增：`const [jdUploading, setJdUploading] = useState(false)` |
  | `src/mock/data.js` | - | 新增 `aiMock.jdFromDoc = { duty: '...', require: '...' }` 模拟上传解析结果 |

- **说明**: 三种来源 = 手填（已有）+ AI 代写（已有）+ 上传 JD 文件（新增），三者都支持 AI 优化。
- **PRD 回写**:

  | 位置 | 编号 | 回写内容 |
  |------|------|---------|
  | 3.1.2 | BR-R-013 后新增 BR-R-013a | 【新增】工作职责与任职要求支持三种录入方式：(1) 手动填写；(2) 上传岗位说明书文件（PDF/Word/图片），AI OCR+语义解析后自动填充；(3) AI 一键代写。三种方式生成的内容均可使用 AI 润色优化 |
  | 3.1.2 交互流程 Step 6 | - | 增加"上传岗位说明书"操作入口 |

---

### B3. AI 代写以企业画像为输入

- **类型**: 🔧调整
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterFlow.jsx` | writeJD #36-45 | 在调用 mock 代写前，先读取 `companyProfile`（`mock/data.js` #291-307），将企业标签/一句话总结作为上下文传入。提示词逻辑改为：mock 时在生成结果开头追加一行说明「基于企业画像（技术驱动·双休·深圳南山）为你生成以下 JD」 |
  | `src/mock/data.js` | aiMock.jdDuty / jdRequire | 内容无需大改（mock 不变），但增加 `aiMock.jdGenContext` 字段展示使用了哪些企业画像信息 |

- **PRD 回写**: 位置 3.1.2 BR-R-017c，修改：【AI】AI-JD 代写以企业画像为输入——生成时默认将企业标签（行业/规模/福利/文化）+ 企业 AI 总结作为 Prompt 上下文，使生成的 JD 体现企业特色。

---

### B4. 发布时生成岗位画像

- **类型**: 🔗串联
- **优先级**: 高
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterFlow.jsx` | RecruiterJobCreate 提交 #139-142 | 在"提交发布"跳转前增加一个「AI 正在生成岗位画像…」的过渡态（1.5s），然后导航到审核页。 |
  | `src/recruiter/RecruiterFlow.jsx` | RecruiterJobReview #173-200 | 审核结果通过后，跳转到岗位画像页 `/recruiter/job-portrait?fresh=true&jobId=new`，展示 AI 刚生成的画像。 |
  | `src/recruiter/RecruiterPortrait.jsx` | RecruiterJobPortrait | 支持 `?fresh=true`——展示"AI 已为你的岗位生成画像"提示。 |

- **PRD 回写**: 位置 3.1.3 交互流程 Step 7 后新增 Step 7a：岗位审核通过上线后，系统自动生成岗位画像（岗位标签+维度评分+AI 画像总结），招聘者可在「岗位→岗位画像」查看。画像包括：技能要求/经验门槛/薪资竞争力/学历要求/岗位热度/招聘紧急度六个维度。

---

### B5. 薪资建议增强

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterFlow.jsx` | showSalaryAI #88-95 | 增加两行展示：(1) 同类岗位市场中位值（如"深圳同类岗位中位薪资 22K"）；(2) "+ 选择对标企业"按钮（点击展开可选几家对标公司如"星辰互联/云创数据/智链科技"），选后 AICard 追加"对标企业中位 24K" |
  | `src/mock/data.js` | aiMock.salarySuggest | 新增字段 `marketMedian: 22, benchmarkCompanies: ['星辰互联', '智链科技', '云创数据'], benchmarkMedian: 24` |

- **PRD 回写**: 位置 3.1.2 BR-R-017b，修改：【AI】薪资建议增强——除市场区间外，增加"同类岗位市场中位值"和"对标单位薪资参考"（招聘者可选 2-5 家同业对标），帮助招聘者评估薪资竞争力。

---

### B6. 审核禁用歧视性用语 ✅

- **类型**: ✅已有
- **不写代码**。`RecruiterFlow.jsx` RecruiterJobReview（#148-211）中的 AI 三级预审已包含歧视性用语检测（#176"仅限男性"示例），覆盖 BR-R-029a。
- **PRD 回写**: 已覆盖（BR-R-029a），无需补充。

---

### B7. 岗位画像可编辑

- **类型**: 🔧改
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterPortrait.jsx` | RecruiterJobPortrait #46-79 | 标签云区每个 tag 旁加"✕"可删除；下方增加"+ 添加标签"输入框；维度评分用 `input[type=range]` 或 +/- 按钮可微调。编辑后底部 toast"画像已更新"。 |
  | `src/mock/data.js` | jobProfile | 保持不变（编辑仅在 UI 层生效，不持久化） |

- **PRD 回写**: 位置 3.1.2 末或 3.1.4，新增 BR-R-034a：【新增】招聘者可编辑 AI 自动生成的岗位画像（标签增删、维度评分微调），编辑后标记"已人工校准"。原 AI 生成值保留用于对比。

---

### B8. 候选人一批人列表 + 匹配度

- **类型**: 🔧扩展
- **优先级**: 高
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterExtra.jsx` | RecruiterCandidate #8-84 | 当前只有单个候选人的匹配分析+对比。需要新增一个「候选人列表」Tab（default）。列表项：头像、姓名/虚拟名、应聘岗位、AI 匹配度(数字+进度条)、核心标签、最近互动时间。点击某候选人→切入现有的单个匹配分析视图。支持多选（checkbox）→对比 |
  | `src/mock/data.js` | - | 新增 `candidateList = [{ id, name, virtual, job, matchScore, tags, lastMsg, emotion, logoIdx }, ...]`，约 5-8 条 |
  | `src/recruiter/RecruiterExtra.jsx` | - | 底部按钮改为：「批量操作」（对选中候选人推送消息/测评/批量推荐） |

- **PRD 回写**:

  | 位置 | 编号 | 回写内容 |
  |------|------|---------|
  | 3.1.6 | 新增小节「3.1.6.x 候选人总览列表」| 描述：招聘者可在候选人分析页查看所有曾留言互动的应聘者列表，按 AI 匹配度降序排列，支持筛选/排序/多选对比/批量操作。列表项含匹配度评分+核心标签+最新互动时间。点击进入单个候选人匹配分析页 |
  | 3.1.6 | BR-R-052c 后新增 BR-R-052d | 【新增】候选人列表页支持：（1）按匹配度/最新互动/岗位筛选排序；(2) 多选候选人进行横向对比（最多 5 人）；(3) 从列表直接进入与候选人的对话 |
  | 5.1.1 | 新增 E023 | candidate_list_view：招聘者浏览候选人列表 |

---

### B9. 浏览量穿透

- **类型**: ➕新增
- **优先级**: 高
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterExtra.jsx` | RecruiterStats #87-122 | 总览统计里的「328 浏览/246 人」数字改为可点击（蓝色链接色），点击跳转到 `/recruiter/job/visitors/:jobId` |
  | 新建 `src/recruiter/RecruiterVisitors.jsx` | - | 新组件：访客列表页。展示浏览过该岗位的所有用户（mock 约 20 人），每人显示：头像/虚拟名/浏览次数/最近浏览时间/AI 匹配度。支持按匹配度降序排列、逐个邀请沟通、一键对这批人批量推送消息 |
  | `src/App.jsx` | - | 新增路由 `/recruiter/job/visitors/:jobId` → `RecruiterVisitors` |
  | `src/mock/data.js` | - | 新增 `jobVisitors = [{ id, name, virtual, views, lastView, matchScore, tags }, ...]`，为每个岗位准备约 20 条访客数据 |

- **PRD 回写**:

  | 位置 | 编号 | 回写内容 |
  |------|------|---------|
  | 3.1.8 | BR-R-073a 后新增 BR-R-073b | 【新增】浏览量穿透：招聘者可点击统计数据中的"UV 人数"数字，进入访客详情列表——展示每个浏览者的概要信息、浏览次数、最近浏览时间、AI 匹配度。支持按匹配度降序排列，支持对单个或批量访客发起邀请沟通 |
  | 5.1.1 | 新增 E024 | visitor_list_view：招聘者点击穿透查看访客列表 |
  | 6.10 验收 | 新增 AC-R-050 | 招聘者在统计页点击"246 人"→进入访客列表→看到每个人信息+匹配度→可发起沟通 |

---

### B10. 岗位排序 + 状态管理

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterPages.jsx` | JobList #30-87 | (1) 岗位卡片上方增加排序按钮组：「按热度」「按时间」「按状态」；(2) 每个岗位增加状态切换：`online` → 可切换为「积极招聘 / 暂停招聘」，`closed` → 可重新上架 |
  | `src/mock/data.js` | myJobs | 每个 job 增加 `recruitStatus: 'active' \| 'paused'`（区别于 `status: 'online' \| 'pending' \| 'closed'`），`active` = 积极招聘（默认），`paused` = 暂停招聘（岗位在线但不积极推送） |

- **PRD 回写**: 位置 3.1.4 或 3.1.8，新增 BR-R-073c：【新增】岗位排序支持按热度（浏览量降序）、按发布时间、按状态筛选。每个在线岗位可设"积极招聘"或"暂停招聘"状态——暂停后岗位仍在线但不进入主动推送。

---

### B11. 总体统计（非单岗位）

- **类型**: ➕新增
- **优先级**: 高
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterExtra.jsx` | RecruiterStats #87-122 | 在单岗位趋势之上增加总览统计卡片：所有岗位的「总浏览量 / 总投递量(留言数) / 总对接成功数 / 平均匹配度」。数据从 `myJobs` 聚合计算 + mock 新增。增加一个"岗位热度 TOP5"迷你排行榜 |
  | `src/mock/data.js` | - | 新增 `aggregateStats = { totalViews: 2847, totalUV: 1916, totalMsgs: 208, totalExchanges: 52, avgMatch: 78, topJobs: [...] }` |

- **PRD 回写**: 位置 3.1.8，BR-R-073c 后新增 BR-R-073d：【新增】总体统计看板：在数据统计页顶部展示所有岗位的聚合数据——总浏览量(PV/UV)、总留言数、总对接成功数、岗位平均匹配度、热度 TOP5 岗位排行。帮助招聘者从全局视角评估招聘效果。

---

### B12. 应聘者专属页面（人才池）

- **类型**: ➕新增（全新一级模块）
- **优先级**: 高
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | 新建 `src/recruiter/RecruiterTalentPool.jsx` | - | 新组件：人才池主页。关键设计——以人为中心而非以岗为中心。**
    - 顶部统计卡片：总应聘者数、本月新增、活跃沟通中、标记为重点的人数
    - 筛选栏：按岗位/按匹配度/按状态/按标签/按最近互动时间
    - 应聘者卡片列表：头像+虚拟名+投递过的岗位数+最近岗位+AI 匹配度+互动状态+标签
    - 点击→进入该应聘者专属详情页（见下方）
    - 支持多选→批量打标签/推送测评 |
  | 新建 `src/recruiter/RecruiterTalentDetail.jsx` | - | 新组件：单个人才详情。聚合展示该应聘者的：基本信息(脱敏)、简历画像概要、投递过的岗位列表(状态)、互动时间线、标签(招聘者可打标) |
  | `src/App.jsx` | - | 新增路由 `/recruiter/talent` → `RecruiterTalentPool`, `/recruiter/talent/:id` → `RecruiterTalentDetail` |
  | `src/recruiter/RecruiterPages.jsx` | RecruiterApp | Tab 增加第 4 个 tab「人才」或 Profile 中增加入口 |
  | `src/mock/data.js` | - | 新增 `talentPool = [{ id, name, virtual, jobs: [{jobId, jobName, status}], matchScore, tags: [], lastActive, totalViews, emotion }, ...]`，约 15-20 条 |

- **PRD 回写**:

  | 位置 | 编号 | 回写内容 |
  |------|------|---------|
  | 3.1 | 新增小节 3.1.10 | **应聘者人才池（按人看）**：作为全新的招聘者端一级模块，人才池以应聘者维度而非岗位维度组织数据。核心功能：总览统计（总应聘者数+活跃数+标记数）、应聘者列表（按岗位/匹配度/状态/标签/互动时间筛选）、单个人才详情页（基本信息+投递岗位+互动时间线+标签管理）、批量操作（推送测评/批量打标） |
  | 3.1.10 | BR-R-090~BR-R-099 | 新编号段：人才池相关业务规则（约 6-10 条） |
  | 5.1.1 | 新增 E025~E027 | talent_pool_view, talent_detail_view, talent_batch_operation 埋点 |
  | 6.10 验收 | 新增 AC-R-060~AC-R-062 | 人才池相关验收场景 |

---

### B13. 推送测评链接

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterTalentPool.jsx` 或 `RecruiterExtra.jsx` | - | 在人才池/候选人列表的多选操作中增加「📋 推送测评」按钮。点击→弹出选择测评类型 Sheet（mock 3 种测评：性格测试/技能测试/综合能力测试）→确认后 toast"已对 N 人推送测评链接" |
  | 也可在 `RecruiterChat.jsx` | 单聊场景 | 对话工具栏增加「推送测评」按钮（单体推送） |

- **PRD 回写**: 位置 3.1.6 或 3.1.10，新增 BR-R-052e：【新增】招聘者可对应聘者推送测评链接：(1) 在对话中单体推送；(2) 在候选人列表/人才池中勾选多人批量推送。推送后应聘者端收到测评通知。一期 mock 测评链接，二期对接实际测评平台。

---

### B14. 双向打标签

- **类型**: ➕新增
- **优先级**: 中
- **代码改动**:

  | 文件 | 位置 | 改动 |
  |------|------|------|
  | `src/recruiter/RecruiterTalentDetail.jsx` | - | 人才详情页底部"我的标签"区域——招聘者可为本应聘者添加自定义标签（输入+回车），每个标签可设为"👁 对方可见"或"🔒 仅自己" |
  | `src/seeker/SeekerFlow.jsx` | SeekerJobDetail / SeekerChat | 对应地，应聘者可在岗位详情或对话中对招聘者/岗位打标签（如"靠谱""回复快""薪资偏低"），同样支持可见性控制 |
  | `src/mock/data.js` | - | 为 talentPool 条目增加 `mutualTags: [{ text, from: 'recruiter'\|'seeker', visible: boolean }]` |

- **PRD 回写**: 位置 3.1.10（人才池）或 3.2.6，新增业务规则：【新增】双向标签：招聘者可对应聘者打标签（如"技术强""沟通好""已面试"），应聘者可对岗位/招聘者打标签（如"已投递""不感兴趣""薪资偏低"）。标签可设置"对方可见"或"仅自己可见"。标签用于辅助 AI 匹配与过滤。

---

### B15. 转发推荐候选人 ✅ 已有

- **不写代码**。`RecruiterExtra.jsx` RecruiterCandidate #16-17 的 `ShareSheet` + #77-78 的「转发推荐」按钮已覆盖。
- **PRD 回写**: 3.1.7 已覆盖（BR-R-060~063），无需补充。

---

### B16. 招聘成效统计 ⏸

- **明确暂不做**。在原型中不做任何改动。
- **PRD 回写**: 在附录 D 或 7.2 二期规划中加一条 AI 功能：#36「AI 招聘成效分析」——预计招几人/实招几人/成本效益分析。当前暂不开发。

---

### B17. 点评机制 ⏸

- **明确暂不做**。在原型中不做任何改动。
- **PRD 回写**: 在 7.2 二期规划"对应聘者/用人单位点评"(已有 P2 项)中加备注：含招聘者评应聘者质量、招聘效率分析、反哺人才库全面性。当前暂不开发。

---

### C1. 研究招聘人员培训 📋

- **非产品功能**，为团队调研待办。
- **PRD 回写**: 无需回写。

---

### C2. 生僻字加拼音注音 📝

- **交互细节**，待定位具体页面后处理。最可能在应聘者/招聘者的姓名展示区域（如聊天页的 `{chat.name[0]}` 头像缩写）。
- **后续处理**: 定位到具体页面后，在 `<span>` 上加 `title` 属性显示全名+拼音即可。
- **PRD 回写**: 无需专门回写。

---

## 三、新路由规划

| 路由 | 组件 | 说明 |
|------|------|------|
| `/seeker/company-portrait/:id` | `SeekerCompanyView` | A1 - 应聘者查看企业画像（复用 recruiter 画像数据） |
| `/recruiter/job/visitors/:jobId` | `RecruiterVisitors` | B9 - 浏览量穿透访客列表 |
| `/recruiter/talent` | `RecruiterTalentPool` | B12 - 人才池主页 |
| `/recruiter/talent/:id` | `RecruiterTalentDetail` | B12 - 单个人才详情 |

## 四、新 Mock 数据清单

| 变量名 | 用途 | 关联需求 |
|--------|------|---------|
| `companyProfiles` (扩展现有) | 多家企业的画像映射 | A1 |
| `resumeOptimizationTips` | AI 简历优化建议 | A9 |
| `seekerProfile.salaryEstimate` | 薪酬测算数据 | A10 |
| `seekerPushStats` | 首页推送统计 | A4 |
| `jobVisitors[]` | 浏览量穿透访客数据 | B9 |
| `aggregateStats` | 招聘者总体统计数据 | B11 |
| `candidateList[]` | 候选人批量列表 | B8 |
| `talentPool[]` | 人才池数据 | B12 |
| `aiMock.jdFromDoc` | 上传 JD 解析结果 | B2 |
| `aiMock.jdGenContext` | 企业画像输入代写上下文 | B3 |
| `aiMock.salarySuggest.marketMedian/benchmark*` | 薪资建议增强 | B5 |
| `mutualTags[]` | 双向标签数据 | B14 |

## 五、文件改动矩阵

| 文件 | A1 | A2 | A3 | A4 | A5 | A6 | A7 | A9 | A10 | A11 | B1 | B2 | B3 | B4 | B5 | B7 | B8 | B9 | B10 | B11 | B12 | B13 | B14 |
|------|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| `App.jsx` | + | | | | | | | | | | | | | | | | | + | | | + | | |
| `seeker/SeekerFlow.jsx` | + | + | + | | + | | | | | | | | | | | | | | | | | | |
| `seeker/SeekerPages.jsx` | | | | + | + | + | + | | | + | | | | | | | | | | | | | |
| `seeker/SeekerExtra.jsx` | + | | | | + | | | | | | | | | | | | | | | | | | |
| `seeker/SeekerPortrait.jsx` | | + | | | | | | + | + | | | | | | | | | | | | | | |
| `recruiter/RecruiterFlow.jsx` | | | | | | | | | | | | + | + | + | + | | | | | | | |
| `recruiter/RecruiterPages.jsx` | | | | | | | | | | | + | | | | | | | | + | | + | | |
| `recruiter/RecruiterExtra.jsx` | | | | | | | | | | | | | | | | | + | + | | + | | + | |
| `recruiter/RecruiterPortrait.jsx` | | | | | | | | | | | + | | | + | | + | | | | | | | |
| `recruiter/RecruiterVisitors.jsx` | | | | | | | | | | | | | | | | | | ✨ | | | | | |
| `recruiter/RecruiterTalentPool.jsx` | | | | | | | | | | | | | | | | | | | | | ✨ | + | + |
| `recruiter/RecruiterTalentDetail.jsx` | | | | | | | | | | | | | | | | | | | | | ✨ | | + |
| `mock/data.js` | + | | | + | + | | + | + | + | | | + | + | | + | | + | + | + | + | + | + | + |
| `styles/wechat.css` | | | | | + | + | | | | | | | | | | | | | + | | | | |

> ✨ = 新建文件，+ = 修改即有文件

## 六、建议提交顺序

| 批次 | 内容 | 改动文件数 | 提交说明 |
|:---:|------|:---:|------|
| 1 | A3（fix unlocked + 投递门槛逻辑） | 2 | 修 bug + 核心逻辑改动，后续需求以此为基础 |
| 2 | A2（补信息页整合简历上传） | 2 | 配合 A3 的投递前置条件 |
| 3 | B8 + B9 + B12（候选人列表+浏览量穿透+人才池） | 3 新建 + 3 修改 | 招聘端全新模块，集中做 |
| 4 | B1 + B2 + B3 + B4 + B7（画像数据流串联） | 4 | 串联已有页面，成本低 |
| 5 | B11 + B5 + B10（统计 + 薪资增强 + 排序状态） | 3 | 改现有页 |
| 6 | A1 + A4 + A5 + A6 + A7（应聘端增强） | 4 | 改现有页 |
| 7 | A9 + A10 + A11（AI 增值） | 2 | 改现有页 |
| 8 | B13 + B14（推送测评+双向标签） | 2 | 依赖人才池（批次 3） |

---

## 七、PRD 回写执行清单（汇总）

| PRD 章节 | 需回写条目 | 对应需求 |
|----------|:---:|------|
| 3.1.1 注册认证 | BR-R-009f（注册后生成企业画像） | B1 |
| 3.1.2 填写空岗信息 | BR-R-013a（上传 JD 文件）、BR-R-017b 修改（薪资增强）、BR-R-017c 修改（代写以画像为输入） | B2, B5, B3 |
| 3.1.3 发布审核 | 交互流程 Step 7a（审核后生成岗位画像） | B4 |
| 3.1.4 修改发布信息 | BR-R-034a（岗位画像可编辑） | B7 |
| 3.1.6 简历查看 | 新小节「候选人总览列表」+ BR-R-052d+e（列表/批量）+ 标题范围扩展 | B8, B13 |
| 3.1.8 数据统计 | BR-R-073b（浏览量穿透）、BR-R-073c（排序状态）、BR-R-073d（总体统计） | B9, B10, B11 |
| 3.1 | 新小节 3.1.10 应聘者人才池 | B12 |
| 3.2.2 订阅关键词 | 修改 BR-A-010（订阅画像概念） | A7 |
| 3.2.3 接收信息 | BR-A-020a（首页推送统计）、BR-A-020b（状态标识）、BR-A-020c（排序筛选） | A4, A5, A6 |
| 3.2.4 补充信息 | BR-A-033a（补信息页上传简历）、BR-A-036 修改（投递前置门槛）、BR-A-038c（AI 简历优化）、BR-A-038d（薪酬测算）、BR-A-036a（企业画像入口） | A2, A3, A9, A10, A1 |
| 3.2.6 留言对话 | BR-A-050 修改（投递需上传简历）、双向标签规则 | A3, B14 |
| 3.2.8 个人主页 | BR-A-071 扩展（测评+学习入口） | A11 |
| BR-X-001 | 加备注"三期中转通讯" | A8 |
| 5.1.1 埋点 | 新增 E022~E027 | A5, B8, B9, B12 |
| 6.x 验收标准 | 新增 AC-A-033, AC-R-050~062 等 | A1, B9, B12 |
| 7.2 二期规划 | B16/B17 加备注"暂不做" | B16, B17 |
| 附录 C 字段清单 | 新增简历文件/标签等字段 | A2, B14 |
| 附录 D AI 清单 | #34 简历解析 P0→一期 | A2 |

---

*方案结束。各批次可独立评审和实现。批次 1（A3）改动最小但影响最大，建议最先做；批次 3（B8+B9+B12）工作量最大，建议作为本轮的标志性交付。*
