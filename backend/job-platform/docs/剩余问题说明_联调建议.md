# 复审修复完成 - 剩余问题说明

修复日期：2026-06-06  
项目：空岗信息发布对接平台

---

## ✅ 阻断问题已全部解决

1. ✅ Alembic ENUM重复创建 - 已修复
2. ✅ pytest全绿（28/28 passed）

---

## ⚠️ 非阻断项但需要注意

### 1. 测试使用SQLite而非PostgreSQL

**现状**：
- pytest使用SQLite in-memory进行测试
- 避免了asyncpg在Windows上的连接池问题

**限制**：
- ✅ 能验证业务逻辑和API流程
- ⚠️ 不能完全验证PostgreSQL特性：
  - ENUM类型行为
  - 索引和约束
  - asyncpg驱动行为
  - PostgreSQL特有函数

**建议**：
```bash
# 联调前在真实PostgreSQL测试库验证
alembic upgrade head
# 确保迁移在真实Postgres上可执行
```

---

### 2. Redis和限流器是Mock的

**现状**：
- 测试环境Mock了所有Redis操作
- RateLimiterV2有单元测试覆盖逻辑

**限制**：
- ✅ 验证了限流逻辑正确性
- ⚠️ 未验证真实Redis：
  - Lua脚本执行
  - 连接池管理
  - 超时策略
  - 网络异常处理

**建议**：
- 联调环境使用真实Redis验证

---

### 3. datetime.utcnow()弃用警告

**现状**：
```python
# app/core/security.py:62
expire = datetime.utcnow() + timedelta(minutes=settings.JWT_EXPIRE_MINUTES)
```

**警告信息**：
```
DeprecationWarning: datetime.datetime.utcnow() is deprecated
```

**不影响运行**，但后续应修改为：
```python
expire = datetime.now(datetime.UTC) + timedelta(minutes=settings.JWT_EXPIRE_MINUTES)
```

---

### 4. 后端API未完整

**已实现**：
- ✅ 认证模块（注册、登录、验证码）
- ✅ 用户模块（查询、更新）
- ✅ 安全基础（加密、限流）

**未实现**（前端原型有，后端无）：
- ⚠️ 岗位管理（CRUD、搜索、推荐）
- ⚠️ 投递管理（投递、查看、状态）
- ⚠️ 聊天消息
- ⚠️ 通知中心
- ⚠️ 简历/企业画像
- ⚠️ 数据统计
- ⚠️ 面试管理

**说明**：
- 前端原型这些页面使用mock数据
- 后续需补充对应后端API

---

### 5. CORS配置需检查

**现状**：
- Vite前端常用端口：5173、5174
- 后端需配置ALLOWED_ORIGINS

**检查清单**：
```python
# backend/.env
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175
```

**验证**：
- 浏览器联调时检查Console
- 如有CORS错误，补充对应源

---

## 🎯 前后端联调建议

### 联调原则

**不要一上来大改UI**  
把当前原型当"交互壳"和"页面蓝图"，先做数据接入层。

---

### 建议顺序

#### 1️⃣ 明确联调目标（优先级排序）

**第一阶段**：认证闭环
- ✅ 发送验证码
- ✅ 注册
- ✅ 登录
- ✅ 获取当前用户
- ✅ 更新用户资料

**第二阶段**：岗位/投递等模块
- ⚠️ 继续使用mock
- ⚠️ 等后端API完成再替换

---

#### 2️⃣ 前端加统一API client

**创建API封装**：
```javascript
// src/services/api.js
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8001'

export async function request(endpoint, options = {}) {
  const token = localStorage.getItem('access_token')
  
  const response = await fetch(`${API_BASE_URL}${endpoint}`, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...(token && { 'Authorization': `Bearer ${token}` }),
      ...options.headers,
    },
  })

  if (response.status === 401) {
    // 清除token，跳转登录
    localStorage.removeItem('access_token')
    window.location.href = '/login'
    throw new Error('未授权')
  }

  if (response.status === 403) {
    throw new Error('无权限')
  }

  if (!response.ok) {
    const error = await response.json()
    throw new Error(error.detail || '请求失败')
  }

  return response.json()
}
```

**配置环境变量**：
```bash
# .env.development
VITE_API_BASE_URL=http://localhost:8001
```

**优势**：
- ✅ 统一处理token
- ✅ 统一处理401/403
- ✅ 统一错误提示
- ✅ 不在每个组件散写fetch

---

#### 3️⃣ 保留mock/real双模式

**架构设计**：
```javascript
// src/services/api.js - 真实API
// src/services/mock.js - Mock数据

// src/services/index.js - 统一导出
const USE_MOCK = import.meta.env.VITE_USE_MOCK === 'true'

export const authService = USE_MOCK ? mockAuthService : realAuthService
export const userService = USE_MOCK ? mockUserService : realUserService
```

**环境变量控制**：
```bash
# 使用真实API
VITE_USE_MOCK=false
VITE_API_BASE_URL=http://localhost:8001

# 使用Mock（原型演示）
VITE_USE_MOCK=true
```

**优势**：
- ✅ 原型演示不依赖后端
- ✅ 联调时切换简单
- ✅ 后端未完成模块继续mock

---

#### 4️⃣ 先接认证闭环

**API清单**：
```javascript
// 认证API
POST /api/v1/auth/send-verification-code?phone=xxx
POST /api/v1/auth/register
POST /api/v1/auth/login

// 用户API
GET  /api/v1/users/me
PUT  /api/v1/users/me
```

**实现步骤**：
1. 创建`src/services/auth.js`
2. 实现注册页面API调用
3. 实现登录页面API调用
4. Token存储到localStorage
5. 实现用户信息获取
6. 实现用户资料更新

**Token管理**：
```javascript
// 登录成功后
localStorage.setItem('access_token', response.access_token)

// 每次请求携带
headers: {
  'Authorization': `Bearer ${localStorage.getItem('access_token')}`
}

// 登出
localStorage.removeItem('access_token')
```

---

#### 5️⃣ 后端准备联调基础

**检查清单**：

1. **数据库迁移**
```bash
# 在真实PostgreSQL测试库执行
alembic upgrade head
# 确保成功，无ENUM重复错误
```

2. **Redis可用**
```bash
# 确认Redis服务运行
redis-cli ping
# 应返回：PONG
```

3. **CORS配置**
```python
# .env
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174
```

4. **环境配置示例**
```bash
# .env.example
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/jobplatform
TEST_DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/jobplatform_test
REDIS_URL=redis://localhost:6379/0
SECRET_KEY=your-secret-key-here
PHONE_HASH_SECRET=your-phone-hash-secret
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174
```

5. **启动命令**
```bash
# 后端启动
cd backend/job-platform
poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8001

# API文档
http://localhost:8001/docs
```

6. **API文档可访问**
- 保持`/docs`可用
- 前端按OpenAPI对齐字段

---

#### 6️⃣ 不要直接污染原型

**保护原型**：
- ✅ 保留当前原型可演示状态
- ✅ 如无Git，先创建Git或复制baseline
- ✅ 联调改动集中在：
  - `src/services/` - API封装
  - 登录注册页面 - API调用
  - 用户状态管理 - Token处理

**避免**：
- ❌ 不要顺手重构全部UI
- ❌ 不要改动未联调模块
- ❌ 不要删除mock数据（保留演示能力）

---

## 📋 给Claude的联调任务口径

```
下一步做前后端联调，不要大范围重构UI。

先把现有原型作为页面壳，新增统一API client，支持VITE_API_BASE_URL和mock/real切换。

优先接入认证闭环：
- 发送验证码
- 注册
- 登录
- 获取当前用户
- 更新当前用户

确认后端准备：
- CORS支持Vite端口
- 真实Postgres执行alembic upgrade head
- Redis可用

岗位、投递、聊天等未完成后端模块继续保持mock，不要硬接假接口。
```

---

## ✅ 总结

### 当前状态

**后端**：
- ✅ 认证和用户模块完整
- ✅ pytest全绿（28/28）
- ✅ Alembic迁移正确
- ⚠️ 使用SQLite测试（联调前需验证真实Postgres）
- ⚠️ Redis是mock（联调时使用真实Redis）

**前端**：
- ✅ 原型UI完整
- ✅ 交互流程清晰
- ⚠️ 使用mock数据
- ⚠️ 需要API接入层

### 下一步

**联调准备**：
1. 后端：验证真实Postgres + Redis
2. 前端：创建API client + 环境变量
3. 优先：认证闭环
4. 后续：逐步替换mock模块

**原则**：
- 渐进式接入
- 保留演示能力
- 不大改UI
- 集中改API层

---

**文档日期**：2026-06-06  
**状态**：可开始联调准备 🚀
