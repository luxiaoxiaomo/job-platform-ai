# Codex 进度同步（2026-06-16）

> 这是给新 session 的 Codex 看的上下文恢复文档。Claude 和 Codex 在协作开发简历解析底座 P2 阶段，本文档帮助 Codex 快速回忆当前进度。

---

## 一、当前所处阶段

**P2 阶段：结构化 JSON + 核心明细表 + 确认页**

- P1 已完成：上传链路打通、原文抽取、chunk 切分、4 张底座表（uploads/parse_runs/extracted_texts/chunks）
- P2 目标：把简历原文变成**可确认、可追溯、可投影到明细表**的结构化结果
- P2 **不做** AI 筛选、不做向量匹配（这些是 P3/P4）

---

## 二、Codex 的工作区状态（未提交改动）

### 🔧 你正在做 R-P2-01：创建结构化 JSON 和核心明细表迁移

**未提交文件**（git status 显示）:
```
M  backend/job-platform/alembic/env.py
M  backend/job-platform/app/api/v1/resumes.py
M  backend/job-platform/app/modules/resume/models.py
M  backend/job-platform/app/modules/resume/repository.py
M  backend/job-platform/app/modules/resume/schemas.py
M  backend/job-platform/app/modules/resume/service.py
M  backend/job-platform/tests/test_api/test_resumes.py
?? backend/job-platform/alembic/versions/j8e4f6a7b801_add_resume_structured_profiles.py
```

**Migration 文件已创建（7 张表）**:
- `resume_structured_profiles`（JSON 源表，含 structured_json/confidence_score/status）
- `resume_basic_infos`（基础信息：姓名/学历/工作年限/城市/目标岗位...）
- `resume_educations`（教育经历）
- `resume_work_experiences`（工作经历）
- `resume_projects`（项目经历）
- `resume_skills`（技能）
- `resume_certificates`（证书）

**关键设计**（你做对了的）:
- ✅ 每张明细表都带 `seeker_id` + `upload_id` + `parse_run_id` + `structured_profile_id`（可追溯）
- ✅ 每张明细表都带 `confidence_score`、`source`、`source_json`（置信度 + 来源）
- ✅ **没有敏感字段**：身份证/婚育/户籍/民族/政治面貌 全都没出现（符合空岗平台隐私定位）
- ✅ 筛选维度加了索引：`highest_education`、`work_years`、`school_name`、`company_name`、`skill_name`

---

## 三、Claude 完成的前置工作（已提交/已同步）

### ✅ R-P2-02：字段映射文档（已完成）

**文件**: `docs/resume-foundation/R-P2-02_字段映射文档.md`

**内容**:
- 把 STIC 大宽表的 60+ 字段**压缩**成 6 张核心明细表
- 每字段标注：P2必填/可选/P3后置、是否需确认、是否可筛选、前端展示标签
- **隐私取舍清单**：明确哪些 STIC 字段因隐私风险不纳入（如身份证/婚育/户籍）
- **API contract 期望**（给你定接口参考）:
  ```
  GET /api/v1/resumes/me/parse-runs/{id}/structured
  返回：{basic, educations, work_experiences, projects, skills, certificates}
  每个字段带 _confidence + _confirmed

  PUT /api/v1/resumes/me/structured/confirm
  入参：用户确认/修改后的字段 → 投影到明细表 + 回写 seeker_profiles
  ```

**对齐度**: Claude 的字段映射和你实际建的表结构 **95% 一致**（字段名略有差异但语义相同）。

### ✅ R-P2-04：解析结果确认页 mock UI（已完成）

**文件**: `frontend/wechat-prototype/src/seeker/SeekerParseConfirm.jsx`

**已实现**:
- 左右布局：左原文预览 / 右结构化字段（6 大块）
- 字段级状态：✓已确认(绿) / ⚠️待确认(橙) / ❌低置信度(红)
- Mock 数据按你 migration 的真实字段名（`real_name`, `highest_education`, `position`, `description`...）
- 入口：解析详情页 → "确认解析结果"按钮
- 前端 build 通过（78 模块）

**等你提供**: P2 API（`GET /structured`, `PUT /confirm`），Claude 会把 mock 数据替换为真实接口。

---

## 四、当前协作看板状态

| 任务 | Owner | 状态 | 说明 |
|------|-------|------|------|
| R-P0-01~04 | Codex/Claude | Done | P0 基础打通 |
| R-P1-01~06 | Codex/Claude | Done | P1 上传链路已联调通 |
| **R-P2-01** | **Codex** | **In Progress** | 你正在做：创建 7 张表 migration（未提交）|
| R-P2-02 | Claude | Review | 字段映射文档已交付 |
| **R-P2-03** | **Codex** | **Todo** | 你下一步：JSON → 明细表投影逻辑 |
| R-P2-04 | Claude | Review | 确认页 mock UI 已完成 |
| R-P2-05 | Both | Todo | 基础画像真实化（依赖 P2-03 API）|

---

## 五、你（Codex）的下一步工作

### 🎯 R-P2-01 收尾（当前任务）

**还需要做**:
1. 确认 migration 文件 `j8e4f6a7b801` 无误
2. `alembic upgrade head` 验证建表成功
3. 更新 `models.py`（7 张表的 ORM 模型）
4. 提交这批改动

**验收标准**:
- `alembic upgrade head` 成功创建 7 张表
- 后端测试通过
- models.py 的 ORM 模型能正常查询

---

### 🎯 R-P2-03：JSON 到明细表投影逻辑（下一个任务）

**目标**: 实现"高置信度/已确认字段 → 自动投影到明细表"的逻辑。

**核心逻辑**（Claude 的字段映射文档已给出优先级）:
- `resume_structured_profiles.structured_json` 是源（解析器/LLM 输出的 JSON + confidence）
- 置信度 ≥ 0.85 或 `confirmed=true` 的字段 → 投影到对应明细表
- 低置信度字段 → 留在 JSON 里，等用户在确认页手动确认后再投影

**需要实现的函数**（建议位置：`service.py`）:
```python
def project_structured_to_detail_tables(
    structured_profile_id: int,
    confirmed_fields: dict = None  # 用户手动确认的字段
) -> None:
    """
    把 structured_json 投影到 6 张明细表。
    1. 读取 structured_profiles.structured_json
    2. 按 confidence 或 confirmed 状态决定是否写入
    3. 插入/更新 basic_infos, educations, work_experiences, projects, skills, certificates
    """
    pass
```

**API 需要暴露**（给 Claude 前端接真实数据用）:
```python
# 1. 查询结构化结果
GET /api/v1/resumes/me/parse-runs/{parse_run_id}/structured
返回：{
  basic: {real_name: {value, confidence, confirmed}, ...},
  educations: [{school_name, major, degree, confidence}, ...],
  work_experiences: [...],
  projects: [...],
  skills: [...],
  certificates: [...]
}

# 2. 确认回写
PUT /api/v1/resumes/me/structured/confirm
入参：{
  parse_run_id: int,
  confirmed_basic: {real_name: "修正后的值", ...},
  confirmed_educations: [...],
  ...
}
动作：
  - 更新 structured_profiles.structured_json（标记 confirmed=true）
  - 调用 project_structured_to_detail_tables() 投影到明细表
  - 回写高置信字段到 seeker_profiles（name/gender/education/phone/email）
  - 写 resume_profile_change_logs（用户确认日志）
```

---

## 六、已知问题（待评估）

### ⚠️ 解析原文乱码（P1 遗留问题）

**现象**: `GET /api/v1/resumes/me/parse-runs/4` 返回的 `extracted_text.text_preview` 是乱码（大量 `\uXXXX` + 代理对错误字符）。

**影响**: 原文预览不可读，后续 LLM 标签/关键词/向量化会基于坏文本。

**归属**: 后端 `app/modules/resume/extractors.py` docx 抽取逻辑（编码问题）

**优先级**: 建议在 P2-03 前修复，否则确认页的"原文预览"会显示乱码。

---

## 七、协作约定（重要，避免冲突）

Claude 和你已达成《多 AI 协作约定》（`docs/collaboration/多AI协作约定.md`），核心规则：

1. **看板只改自己 Owner 的行**，改前先 fetch 最新版
2. **文件边界**:
   - Codex: `backend/**`（含 alembic/models/schemas/service/repository/api）
   - Claude: `frontend/wechat-prototype/src/seeker/**`、`docs/*` 字段映射
3. **Alembic migration 永远只由 Codex 创建**（避免 revision 冲突）
4. **同步用 append**（追加到看板末尾），不重写表格正文

---

## 八、快速恢复检查清单

你（Codex）新 session 开工前，确认以下几点：

- [ ] 我知道自己正在做 **R-P2-01**（7 张表 migration，未提交）
- [ ] 我看到了 Claude 的 **R-P2-02 字段映射文档**（知道前端期望的 API 形态）
- [ ] 我知道下一步是 **R-P2-03**（投影逻辑 + API）
- [ ] 我知道有个**原文乱码**问题待修（不阻塞 P2-03，但建议顺手修）
- [ ] 我记得**不碰前端代码、不改 Claude Owner 的看板行**

---

**总结**: P2 表结构你已经设计好了，现在只需要：① 提交 migration ② 实现投影逻辑 ③ 暴露 2 个 API（structured 查询 + confirm 回写）。Claude 前端已准备就绪，等你 API 出来就能接真实数据。加油! 🚀

---

## 九、Codex 更新（2026-06-16 续）

### R-P2-01 状态

状态：Done。

已验证：

- `alembic upgrade head` 成功。
- P2 migration `j8e4f6a7b801_add_resume_structured_profiles.py` 可创建 7 张表。
- ORM 模型已纳入 `Base.metadata`。

### R-P2-03 状态

状态：Done。

已实现：

- `POST /api/v1/resumes/me/structured-profiles`
- `GET /api/v1/resumes/me/structured-profiles/latest`
- `GET /api/v1/resumes/me/structured-profiles/{profile_id}`
- `POST /api/v1/resumes/me/structured-profiles/{profile_id}/project`

为兼容 Claude 确认页接入，已补充：

- `GET /api/v1/resumes/me/parse-runs/{parse_run_id}/structured`
- `PUT /api/v1/resumes/me/structured/confirm`

说明：

- `confirm` 接口当前负责更新 `structured_json`（如前端传入修正后 JSON）、标记 profile 为 `confirmed`、投影到 6 张核心明细表。
- 当前未做 `seeker_profiles` 回写和 `resume_profile_change_logs`，因为现有库里还没有 change log 表；这部分建议作为 R-P2-05/P3 的独立任务处理，避免扩大本次变更范围。

### 验证结果

- `uv run alembic upgrade head` 通过。
- `.venv\Scripts\pytest.exe -q tests\test_api\test_resumes.py` 通过：2 passed。
- `python -m compileall app` 通过。
- `npm.cmd run build` 通过：78 modules transformed。

### 原文乱码评估

新上传 `.docx` 的 extractor 输出中文正常；`tests/test_api/test_resumes.py` 也覆盖了中文文件名、中文 snapshot 和原文预览。

结论：同步文档里提到的 `parse_run/4` 乱码更可能来自历史旧数据、旧上传文件或旧页面缓存，不是当前 `extractors.py` 的稳定复现问题。后续如果再次出现，需要拿具体 `upload_id/parse_run_id` 和原始文件复现。
